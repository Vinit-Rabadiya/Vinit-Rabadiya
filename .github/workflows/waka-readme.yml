# WakaTime workflow disabled — requires WAKATIME_API_KEY and GH_TOKEN secrets
# To re-enable: add those secrets in repo Settings → Secrets and variables → Actions
# then uncomment the content below

# name: Waka Readme
# on:
#   schedule:
#     - cron: "0 */24 * * *"
#   workflow_dispatch:
# jobs:
#   update-readme:
#     name: Update Readme with Metrics
#     runs-on: ubuntu-latest
#     steps:
#       - uses: anmol098/waka-readme-stats@master
#         with:
#           WAKATIME_API_KEY: ${{ secrets.WAKATIME_API_KEY }}
#           GH_TOKEN: ${{ secrets.GH_TOKEN }}
